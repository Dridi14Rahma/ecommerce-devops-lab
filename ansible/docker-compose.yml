version: "3"

services:
  nginx:
    image: nginx
    ports:
      - "80:80"

  nodeapp:
    image: node:18
    working_dir: /app
    command: node server.js

  mongo:
    image: mongo
    ports:
      - "27017:27017"
