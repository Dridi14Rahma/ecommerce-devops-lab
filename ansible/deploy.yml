---
- name: Deploy E-Commerce App
  hosts: web
  become: yes
  gather_facts: no
  
  # NE PAS utiliser Python 3.11 au démarrage - utiliser le défaut!
  # Python 3.11 sera disponible après le user_data Terraform

  pre_tasks:
    - name: Wait for SSH port to be available
      wait_for:
        host: "{{ inventory_hostname }}"
        port: 22
        delay: 30
        timeout: 600
      vars:
        ansible_connection: local
        ansible_python_interpreter: auto_legacy_group_vars

    - name: Check if Python 3.11 is available
      raw: which python3.11
      register: python311_check
      ignore_errors: yes
      retries: 30
      delay: 10
      until: python311_check.rc == 0

    - name: Ensure Python 3.11 is installed (if not found)
      raw: |
        if ! which python3.11 >/dev/null 2>&1; then
          sudo yum update -y
          sudo yum install -y python3.11
        fi
      when: python311_check.rc != 0

    - name: Set Python interpreter to 3.11
      set_fact:
        ansible_python_interpreter: /usr/bin/python3.11

  vars:
    ansible_user: ec2-user

  tasks:

    - name: Gather Facts
      setup:

    - name: Update system packages
      yum:
        name: "*"
        state: latest

    - name: Install required packages
      yum:
        name:
          - docker
          - git
          - curl
        state: present

    - name: Start Docker
      service:
        name: docker
        state: started
        enabled: yes

    - name: Wait for Docker daemon to be ready
      command: docker ps
      register: docker_ready
      retries: 5
      delay: 10
      until: docker_ready.rc == 0

    - name: Add ec2-user to docker group
      user:
        name: ec2-user
        groups: docker
        append: yes

    - name: Verify Docker Compose is installed
      command: which docker-compose
      register: docker_compose_check
      ignore_errors: yes

    - name: Install Docker Compose if not present
      get_url:
        url: https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64
        dest: /usr/local/bin/docker-compose
        mode: "0755"
      when: docker_compose_check.rc != 0

    - name: Create app directory
      file:
        path: /home/ec2-user/app
        state: directory
        owner: ec2-user
        group: ec2-user
        mode: "0755"

    - name: Copy docker-compose file
      copy:
        src: docker-compose.yml
        dest: /home/ec2-user/app/docker-compose.yml
        owner: ec2-user
        group: ec2-user
        mode: "0644"

    - name: Copy nginx config
      copy:
        src: nginx.conf
        dest: /home/ec2-user/app/nginx.conf
        owner: ec2-user
        group: ec2-user
        mode: "0644"

    - name: Copy server files
      copy:
        src: server/
        dest: /home/ec2-user/app/server/
        owner: ec2-user
        group: ec2-user
        mode: "0755"

    - name: Reset SSH connection to apply docker group changes
      meta: reset_connection

    - name: Run containers
      shell: |
        cd /home/ec2-user/app
        docker-compose up -d
      become_user: ec2-user
      environment:
        PATH: "{{ ansible_env.PATH }}"
      retries: 5
      delay: 10
      register: compose_result
      until: compose_result.rc == 0

    - name: Wait for containers to start
      pause:
        seconds: 30

    - name: Wait for application to be ready
      uri:
        url: "http://localhost"
        status_code: 200
      retries: 10
      delay: 10
      register: result
      until: result.status == 200
      ignore_errors: yes

    - name: Display deployment status
      debug:
        msg: "Application deployed successfully on {{ inventory_hostname }}"
